# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# SSO Auth Server - Environment Variables for Vercel
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 
# IMPORTANT: Add these to Vercel Dashboard → Project Settings → Environment Variables
# Scope: Production
#
# Copy these values to Vercel (DO NOT commit this file with real values)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔑 Core Authentication Settings (REQUIRED)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Database connection string (PostgreSQL)
# Get from Neon dashboard: https://console.neon.tech
DATABASE_URL=postgresql://user:password@host.region.aws.neon.tech/dbname?sslmode=require

# Secret key for JWT signing and encryption (MUST be 32+ characters)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-minimum-32-characters-long

# Auth server URL (backend endpoint) - USE YOUR NEW DOMAIN
BETTER_AUTH_URL=https://auth-imsanghaar-agentfactory.vercel.app

# Auth server URL (client-side, publicly accessible) - USE YOUR NEW DOMAIN
NEXT_PUBLIC_BETTER_AUTH_URL=https://auth-imsanghaar-agentfactory.vercel.app

# Environment mode
NODE_ENV=production

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🌐 CORS & Client Configuration (REQUIRED)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Allowed origins for CORS (comma-separated, no spaces)
# Include ALL domains that will authenticate through this server:
# - Your book/learn app
# - Your auth server itself (for admin panel, etc.)
ALLOWED_ORIGINS=https://agentfactory-imsanghaar.vercel.app,https://auth-imsanghaar-agentfactory.vercel.app

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🎨 Branding & White-Label Configuration (OPTIONAL)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Application name (shown in UI headers and page titles)
NEXT_PUBLIC_APP_NAME=imsanghaar SSO

# Application description (shown in UI subtitles)
NEXT_PUBLIC_APP_DESCRIPTION=Secure Single Sign-On

# Organization name (shown in footers, emails, security messages)
NEXT_PUBLIC_ORG_NAME=imsanghaar

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔗 Continue URL (OPTIONAL)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Where users go after authentication (used for "Continue to Application" button)
NEXT_PUBLIC_CONTINUE_URL=https://agentfactory-imsanghaar.vercel.app

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📧 Email Configuration (OPTIONAL but RECOMMENDED for production)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Option 1: Resend API (Recommended)
# Get API key from: https://resend.com/api-keys
# RESEND_API_KEY=re_xxxxxxxxx
# EMAIL_FROM=noreply@yourdomain.com

# Option 2: Gmail SMTP (Fallback)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-backup@gmail.com
# SMTP_PASS=your-16-char-app-password
# SMTP_SECURE=false

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# ⚡ Redis Rate Limiting (OPTIONAL - for multi-instance deployments)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# REDIS_URL=https://your-redis.upstash.io
# REDIS_TOKEN=your-redis-token

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📋 Audit Logging (OPTIONAL - for compliance)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# ENABLE_AUDIT_LOGGING=false

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# ✅ SETUP CHECKLIST
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# 1. Go to Vercel Dashboard → auth-imsanghaar-agentfactory project
# 2. Navigate to Settings → Environment Variables
# 3. Add ALL required variables above (scope: Production)
# 4. Click "Redeploy" to apply changes
# 5. Test sign-in and sign-up flows
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
